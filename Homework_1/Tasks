import math

# 1. Напишите программу, которая принимает на вход цифру, обозначающую день недели,
# и проверяет, является ли этот день выходным.

# Пример:
# - 6 -> да
# - 7 -> да
# - 1 -> нет

num = int(input("Введите день недели: "))
if 6 > num >= 1:
    print(f"{num} -> нет")
elif num == 6 or num == 7:
    print(f"{num} -> да")
else:
    print("Введите число в диапазоне от 1 до 7")


# 2. Напишите программу для. проверки истинности утверждения
# ¬(X ⋁ Y ⋁ Z) = ¬X ⋀ ¬Y ⋀ ¬Z для всех значений предикат.

for x in range(0, 2):
    for y in range(0, 2):
        for z in range(0, 2):
            print(
                f"При X = {x} Y = {y} Z = {z} - {not(x or y or z)==(not x and not y and not z)}")


# 3. Напишите программу, которая принимает на вход координаты точки (X и Y),
# причём X ≠ 0 и Y ≠ 0 и выдаёт номер четверти плоскости, в которой находится эта точка (или на какой оси она находится).
# Пример:
# - x=34; y=-30 -> 4
# - x=2; y=4-> 1
# - x=-34; y=-30 -> 3

x = int(input("Введите координату Х: "))
y = int(input("Введите координату Y: "))
if x > 0 and y > 0:
    print(
        f"Точка с координатами (X = {x} и Y = {y}) находится в 1-й четверти плоскости")
elif x < 0 and y > 0:
    print(
        f"Точка с координатами (X = {x} и Y = {y}) находится вo 2-й четверти плоскости")
elif x < 0 and y < 0:
    print(
        f"Точка с координатами (X = {x} и Y = {y}) находится в 3-й четверти плоскости")
elif x > 0 and y < 0:
    print(
        f"Точка с координатами (X = {x} и Y = {y}) находится в 4-й четверти плоскости")
else:
    print("Координаты Х и Y не должны ровняться 0!")


# 4. Напишите программу, которая по заданному номеру четверти,
# показывает диапазон возможных координат точек в этой четверти (x и y).

num = int(input("Введите номер четверти: "))
if num == 1:
    print("Диапозон возможных координат точки: Х = от 0 до + бесконечности, Y = от 0 до + бесконечности")
elif num == 2:
    print("Диапозон возможных координат точки: Х = от 0 до - бесконечности, Y = от 0 до + бесконечности")
elif num == 3:
    print("Диапозон возможных координат точки: Х = от 0 до - бесконечности, Y = от 0 до - бесконечности")
elif num == 4:
    print("Диапозон возможных координат точки: Х = от 0 до + бесконечности, Y = от 0 до - бесконечности")


# 5. Напишите программу, которая принимает на вход координаты двух точек и
# находит расстояние между ними в 2D пространстве.
# Пример:
# - A (3,6); B (2,1) -> 5,09
# - A (7,-5); B (1,-1) -> 7,21

print("Введите координаты первой точки: ")
x1 = int(input("Х1: "))
y1 = int(input("Y1: "))
print("Введите координаты второй точки: ")
x2 = int(input("Х2: "))
y2 = int(input("Y2: "))
resalt = math.sqrt(math.pow((x2-x1), 2) + math.pow((y2-y1), 2))
print(f"A ({x1}, {y1}); B ({x2}, {y2}) -> {round(resalt, 2)}")
